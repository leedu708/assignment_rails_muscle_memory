<%= form_for(@article) do |f| %>

<div class="field">
    <h3><%= f.label "Article Title:" %></h3>
    <%= f.text_field :title %>
</div>

<div class="field">
  <h3><%= f.label "Article Content:" %></h3>
  <%= f.text_area :body %>
</div>

<div class="actions">
  <%= f.submit %>
</div>

<% end %>